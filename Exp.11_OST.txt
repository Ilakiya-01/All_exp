INTEGRATE GIT WITH DESKTOP AND MAVEN

git --version
2.git config --global user.name "Deepika"
3.git config --global user.email "deepikaperumal0405@gmail.com"
git clone repooooo
git add file.name
git status
git commit -m "done"
git remote add origin url
git push origin current branch
then check on website 
then clone same commands then  checkout branch
if you are changing branch name git checkout -b name
add txt2
git push origin current branch
